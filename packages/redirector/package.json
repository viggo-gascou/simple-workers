{
  "name": "redirector",
  "version": "1.1.0",
  "private": true,
  "scripts": {
    "test": "vitest",
    "dev": "node --env-file=.env scripts/generate-config.mjs && wrangler dev",
    "build": "node --env-file=.env scripts/generate-config.mjs",
    "cf-deploy": "wrangler deploy",
    "cf-typegen": "wrangler types"
  }
}
