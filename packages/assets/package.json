{
  "name": "assets",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "test": "vitest",
    "dev": "node scripts/generate-config.mjs && wrangler dev",
    "build": "node scripts/generate-config.mjs",
    "cf-deploy": "wrangler deploy",
    "cf-typegen": "wrangler types"
  }
}
