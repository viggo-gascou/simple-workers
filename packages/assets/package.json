{
  "name": "assets",
  "version": "1.0.0",
  "private": true,
  "dependencies": {
    "@simple-workers/shared": "workspace:*"
  },
  "scripts": {
    "test": "vitest",
    "dev": "wrangler dev",
    "cf-deploy": "wrangler deploy",
    "cf-typegen": "wrangler types"
  }
}
